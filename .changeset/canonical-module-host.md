---
'@ankhorage/orchestrator-module-expo-google-fonts': minor
---

Generate Google Fonts runtime files under the canonical `src/modules/google-fonts` namespace and update layout imports accordingly. Add an optional package-owned host contribution with generic metadata, layout integration, and config normalization while keeping the root Orchestrator module standalone and avoiding a fake configuration form.
